import {StyleSheet} from "react-native";
import {Config} from "../../config/theme";

export const styles = StyleSheet.create({
  mainComponent: {
    backgroundColor: Config.colors.base.gray600,
    flex: 1,
  },
  container: {
    flex: 1,
  },
});
